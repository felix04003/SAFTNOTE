'use strict';
// Worker de génération des bulletins PDF via Puppeteer
// TODO : Template HTML du bulletin + upload MinIO
require('dotenv').config();
const logger = require('../utils/logger');
logger.info('Worker génération-bulletins — à implémenter (Phase 3)');
